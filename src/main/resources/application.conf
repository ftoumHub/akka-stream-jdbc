#config-postgres
# Load using SlickSession.forConfig("slick-postgres")
demo-db {
  profile = "slick.jdbc.PostgresProfile$"
  db {
    dataSourceClass = "slick.jdbc.DriverDataSource"
    properties = {
      driver = "org.postgresql.Driver"
      url = "jdbc:postgresql://localhost:5432/slickdemo"
      user = "postgres"
      password = "postgres"
    }
  }
}
#config-postgres